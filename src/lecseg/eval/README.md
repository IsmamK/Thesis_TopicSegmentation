# lecseg.eval

Evaluation metrics. Pk, WindowDiff, BoundaryS, hierarchical variants, and tolerance-F1.

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.eval import *"` exits with no error.
- [ ] Unit tests live in `tests/test_eval.py`.
