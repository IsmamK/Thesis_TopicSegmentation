# lecseg.refine

LLM refinement. Uses a local Ollama model to adjust boundary positions and generate chapter titles (N4).

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.refine import *"` exits with no error.
- [ ] Unit tests live in `tests/test_refine.py`.
