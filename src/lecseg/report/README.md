# lecseg.report

Report generation. Converts metric dictionaries into LaTeX tables and Markdown summaries.

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.report import *"` exits with no error.
- [ ] Unit tests live in `tests/test_report.py`.
