# lecseg.viz

Visualisation. Produces timeline plots, metric heatmaps, and ablation bar charts.

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.viz import *"` exits with no error.
- [ ] Unit tests live in `tests/test_viz.py`.
