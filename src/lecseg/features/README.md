# lecseg.features

Feature extraction. Computes text embeddings (SBERT/E5/BGE), CLIP visual embeddings, and prosody vectors.

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.features import *"` exits with no error.
- [ ] Unit tests live in `tests/test_features.py`.
