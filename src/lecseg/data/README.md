# lecseg.data

Raw data access helpers. Provides paths and loaders for every intermediate data artifact.

## How to know you are done

- [ ] All public functions have a one-line docstring.
- [ ] Module is importable: `python -c "from lecseg.data import *"` exits with no error.
- [ ] Unit tests live in `tests/test_data.py`.
